# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1

# --- LLM (Groq / Llama 3.3 70B) ---
groq==0.13.1

# --- OpenAI (only for the embedding benchmark: text-embedding-3-*) ---
openai==1.59.6

# --- Embeddings (local, CPU) ---
sentence-transformers==3.3.1

# --- Vector store + hybrid search ---
# chromadb 1.x ships prebuilt wheels (Rust core) on Windows/macOS/Linux for
# Python 3.12 — no C++ compiler needed, unlike the older 0.5.x + chroma-hnswlib.
chromadb==1.0.21
rank-bm25==0.2.2
numpy==1.26.4

# --- Tools ---
duckduckgo-search==6.3.7

# --- Observability ---
langfuse==2.57.0

# --- Eval ---
scikit-learn==1.6.0
